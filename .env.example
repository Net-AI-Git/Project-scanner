# Copy to .env and fill in. Do not commit .env.

# Optional: GitHub token for API rate limit (5000/h). Required for real integration tests.
# Create at: https://github.com/settings/tokens (no scope needed for public repos)
GITHUB_TOKEN=

# LLM: Google AI Studio (Gemini). Get key at https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Optional: override model (e.g. gemini-1.5-flash, gemini-1.5-pro, gemini-2.0-flash)
# GOOGLE_MODEL=gemini-2.0-flash

# Fallback: Nebius Token Factory (e.g. for evaluators)
NEBIUS_API_KEY=
